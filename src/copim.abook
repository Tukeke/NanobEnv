#!/bin/bash
source /etc/copim.conf
[[ ! -e ~/.copim/abook ]] && {  mkdir -p ~/.copim/abook/ ; cp $PREFIX/share/copim/abook.cfg ~/.copim/abook/; } &>/dev/null
abook --datafile ~/.copim/abook/addressbook -C ~/.copim/abook/abook.cfg
